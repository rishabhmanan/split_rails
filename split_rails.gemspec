Gem::Specification.new do |s|
  s.authors = ["Rishabh Manan"]
  s.files = Dir["lib/**/*.rb"]
  s.name = "split_rails"
  s.summary = "A gem to split an array of integers based on even and odd integers"
  s.version = "1.0.0"
end
